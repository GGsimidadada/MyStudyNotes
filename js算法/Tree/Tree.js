!function (global) {
  // 树形结构包含一系列存在父子关系的节点。每个节点都有一个父节点以及零个或多个子节点
  // 二叉树的节点最多只能有两个子节点。
  // 二叉搜索树是二叉树的一种，但是它只允许在左侧节点存储比父节点小的值，在右侧存储比父节点大的值。

  // 创建二叉搜索树类
  const _key = Symbol();
  const _left = Symbol();
  const _right = Symbol();
  class BinarySearchTree {
    constructor () {
      
    }
  }
}(window)